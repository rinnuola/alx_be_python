# Define a function that takes two parameters, length and width. Inside the function, multiply the length and width to calculate the area. Use the return statement to return the calculated area.
def rectangle_area(length, width):
    """Calculate the area of the rectangle"""
    return length * width
#Test
area = rectangle_area(10, 5)
print(f"The area of the rectangle is {area}")